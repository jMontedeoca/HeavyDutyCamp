import { Commitment, Connection, VersionedTransaction } from '@solana/web3.js';
export interface SendAndConfirmTransaction {
    connection: Connection;
    transaction: VersionedTransaction;
    latestBlockhash: {
        blockhash: string;
        lastValidBlockHeight: number;
    };
    commitment?: Commitment;
}
export declare function sendAndConfirmTransaction(params: SendAndConfirmTransaction): Promise<string>;
